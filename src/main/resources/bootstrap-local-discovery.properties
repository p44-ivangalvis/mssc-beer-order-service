#This enables to bring up the config service so that we can bootstrap it and enable discovery of the properties set in the
#config service that are related to the beer service
spring.cloud.discovery.enabled=true
spring.cloud.config.discovery.service-id=mssc-brewery-config